id: io.github.jeromea.gcheckers
runtime: org.gnome.Platform
runtime-version: '50'
sdk: org.gnome.Sdk
command: gcheckers
finish-args:
  - --device=dri
  - --share=ipc
  - --share=network
  - --socket=fallback-x11
  - --socket=wayland
modules:
  - name: gcheckers
    buildsystem: simple
    build-commands:
      - make install-checkers install-schemas PREFIX=/app
    sources:
      - type: dir
        path: .

A checkers program.

Dependencies:

  - glib-2.0 development headers (pkg-config is used to locate it)
  - Xvfb and ImageMagick for screenshot automation (run ./tools/setup.sh on Debian/Ubuntu)

Build the library and CLI:

  make

Run the text-mode game:

  ./checkers
